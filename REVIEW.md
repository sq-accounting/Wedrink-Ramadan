# WeDrink басқару панелі — кәсіби код-ревью және жақсарту жоспары

**Файл:** `index.html` (бұрынғы `wedrink5.html`), ~3018 жол, бір файлды SPA
**Технология:** таза HTML + CSS + JavaScript (build жоқ), localStorage, Tesseract.js (OCR), Supabase REST (синхрондау), Anthropic/Gemini (AI көмекші)
**Тіл:** интерфейс қазақша, тексеру күні: 2026-06-22

---

## 1. Жалпы баға

Қосымша жоғары деңгейде жасалған: екі кафеге арналған толыққанды басқару жүйесі — дашборд, қаржы, айлық есеп (PDF), кадр, активтер, қойма, каталог, CRM, OCR чек оқу, AI көмекші, бұлтпен синхрондау және рөлге негізделген авторизация. Код таза құрылымдалған, `escapeHtml` арқылы XSS-тен негізінен қорғалған, responsive дизайн мұқият ойластырылған, басып шығару (print → PDF) дұрыс істейді.

**Негізгі тәуекелдер:** (1) барлығы бір 3000 жолдық файлда — қолдау қиын; (2) автоматтандырылған тест жоқ; (3) деректер тек браузерде (localStorage) — жоғалу қаупі; (4) OCR парсерлері нақты чек форматына тәуелді, сынғыш.

---

## 2. Табылған қателер және түзетілгені

Осы ревью аясында **түзетілген** нақты қателер (`index.html`):

| # | Қате | Сипаттама | Статус |
|---|------|-----------|--------|
| 1 | **Уақыт белдеуі (timezone)** | `isoMonth()` мен `todayISO()` `toISOString()` (UTC) қолданған. UTC+5 (Түркістан) үшін `new Date(2026,0,1)` → "2025-12" береді: алдыңғы/келесі айға ауысқанда **қаңтар орнына желтоқсан** көрсетілуі мүмкін, кешкі енгізілген операциялар келесі/алдыңғы күнге жазылуы мүмкін. Жергілікті уақытпен есептеуге ауыстырылды. | ✅ Түзетілді |
| 2 | **`tenure()` "0 ай"** | Жаңа қызметкердің өтілі "1 айдан аз" орнына "0 ай" болып шыққан (оператор приоритеті қатесі). | ✅ Түзетілді |
| 3 | **Жоқ Gemini модельдері** | Тізімде `gemini-3-flash`, `gemini-3.5-flash` болған — олар жоқ, API 404 қайтарады. Шынайы `gemini-2.5-pro`, `gemini-2.0-flash`-пен ауыстырылды. | ✅ Түзетілді |
| 4 | **CSV формула-инъекциясы** | Экспортта `=`, `+`, `-`, `@`-пен басталатын ұяшықтар Excel-де формула ретінде орындалуы мүмкін еді (қауіпсіздік). `csvCell()` көмекшісі қосылды. | ✅ Түзетілді |
| 5 | **Жад ағуы (memory leak)** | `URL.createObjectURL()` ешқашан `revoke` етілмеген (JSON/CSV/каталог экспорты). `downloadBlob()` ортақ көмекшісі қосылды. | ✅ Түзетілді |
| 6 | **`addCafe` — addr жоқ** | Жаңа кафеге `addr` өрісі берілмеген, OCR авто-анықтауы үшін қажет. | ✅ Түзетілді |
| 7 | **`escapeHtml` `'` экрандамаған** | Бір тырнақша экрандалмаған — атрибуттарда теориялық тәуекел. Қазір `&#39;` ретінде экрандалады. | ✅ Түзетілді |
| 8 | **`fmt(NaN)`** | Дұрыс емес сан "NaN" болып көрінуі мүмкін еді — енді 0 қайтарады. | ✅ Түзетілді |
| 9 | **Қос `active` навигацияда** | `entry` мен `dash` түймелерінде бір уақытта `active` класы болған. | ✅ Түзетілді |

---

## 3. Назар аударуды қажет ететін мәселелер (түзетілмеген — талқылау керек)

### Архитектура / қолдау
- **Бір 3000-жолдық файл.** CSS, HTML, JS араласқан. Модульдерге бөлу (немесе ең болмаса CSS/JS-ті жеке файлдарға шығару) қолдауды жеңілдетеді.
- **Тест жоқ.** Парсерлер (`parseSalesReceipt`, `parseSupplyReceipt`, `parseCatalogInvoice`), қаржы есептеулері (`sumTx`, маржа), `tenure` — бәрі тестсіз. Регрессия қаупі жоғары.
- **Кодтың қайталануы.** Шығыс құрылымы (breakdown) `renderDash`, `renderReport`, `printReport`-та үш рет қайталанады. Жалпы функцияға шығаруға болады.

### Деректер және сенімділік
- **localStorage — жалғыз дереккөз.** Браузер кэшін тазалау = бүкіл бизнес-дерек жоғалады. Supabase синхрондау бар, бірақ міндетті емес. Кемінде автоматты ескерту/еске салу қажет.
- **Синхрондау конфликт-стратегиясы әлсіз.** `rev=Date.now()` — екі құрылғы бір уақытта жазса, "last write wins", дерек жоғалуы мүмкін. RLS саясаты `using(true) with check(true)` — кез келген `anon key` бар адам бүкіл деректі оқып/жаза алады.
- **Авторизация өшірулі** (`AUTH_ENABLED=false`). Бүкіл логин/рөл жүйесі дайын, бірақ белсенді емес — қазір кез келген ашқан адам толық қол жеткізеді. Деплой алдында қосу керек.
- **Парольдің fallback хэші әлсіз** (file:// үшін FNV). Тек SHA-256 (HTTPS) сенімді. Бұл нақты аутентификация үшін жеткіліксіз — backend қажет.

### Қауіпсіздік
- **API кілттері localStorage-та** (Anthropic, Gemini, Supabase). Бұл құжатталған, бірақ XSS болса кілттер ұрланады. Supabase Edge Function (proxy) нұсқасы дұрыс бағыт — оны әдепкі ету ұсынылады.
- **Anthropic браузерден тікелей** (`anthropic-dangerous-direct-browser-access`) — кілтті әшкерелейді әрі CORS сынғыш. Production-да тек proxy арқылы шақыру керек.

### UX / қолжетімділік (a11y)
- `prompt()`/`confirm()` (қойма қозғалысы, келу қосу, өшіру) — мобильде ыңғайсыз, дизайнмен үйлеспейді. Модаль терезелерге ауыстырған жөн.
- **Форма label-дары `for`/`id`-мен байланыспаған** — экран оқу құралдары үшін нашар.
- SVG иконкаларда `aria-hidden`/`aria-label` жоқ.
- OCR ұзақ процесте (catalog: rus+chi_sim жүктеу) толық экранды бөгейтін индикатор жоқ — қолданушы қатып қалды деп ойлауы мүмкін.

### Өнімділік
- Әр `render()` бүкіл секцияны `innerHTML`-мен қайта құрады. Дерек көп болса (мыңдаған операция) баяулайды. Виртуализация/инкременттік жаңарту керек болуы мүмкін.
- Tesseract.js әр чекте жаңа worker құрып, `terminate` етеді — worker-ді қайта пайдалану жылдамдатады.

---

## 4. Жақсарту жоспары (басымдық бойынша)

### Кезең 1 — Тұрақтылық (бірден)
1. ✅ Жоғарыдағы 9 қатені түзету (орындалды).
2. **Деректі сақтау сенімділігі:** localStorage толып кетсе/қолжетімсіз болса ескерту; апта сайын автоматты JSON backup еске салу.
3. **Парсерлерге unit-тест** қосу (нақты чек мысалдарымен). Vitest немесе бір қарапайым тест-runner.

### Кезең 2 — Қауіпсіздік пен көпқолданушылық (деплой алдында)
4. **`AUTH_ENABLED=true`** қосып, рөлдерді нақты тексеру.
5. Supabase **RLS саясатын қатайту** (workspace + JWT бойынша), `anon key`-ке шектеу.
6. AI шақыруларын **тек Supabase Edge Function (proxy)** арқылы жүргізу — кілт серверде.
7. Синхрондауда **нақты конфликт шешу** (per-record нұсқалау немесе CRDT, ең болмаса updated_at салыстыру).

### Кезең 3 — Сапа мен қолдау
8. Файлды **модульдерге бөлу:** `styles.css`, `app.js` (немесе Vite + ES modules). Бір HTML артықшылығын сақтау керек болса — build қадамымен бір файлға жинау.
9. **Кодтың қайталануын жою:** breakdown/report рендерін ортақ функцияға шығару.
10. `prompt`/`confirm`-ды **модаль терезелерге** ауыстыру.

### Кезең 4 — Мүмкіндіктер мен UX
11. **Қолжетімділік:** label байланыстыру, focus-trap (модаль), `aria` атрибуттары, пернетақтамен навигация.
12. **OCR жақсарту:** worker-ді кэштеу, прогресс-индикаторды бірыңғайлау, парсерге сенімділік деңгейі (confidence) қосу.
13. **Аналитика:** күндік/апталық тренд, болжам (forecast) дәлдігін арттыру, кафелерді салыстыру графигі.
14. **PWA:** офлайн жұмыс, телефонға "орнату", push-ескерту (қойма азайғанда).

---

## 5. Қорытынды

Код деңгейі жоғары әрі өнімге жақын. Осы ревьюде **9 нақты қате түзетілді** (ең маңыздысы — уақыт белдеуі мен жоқ AI модельдері). Деплойға дейінгі басты міндеттер: **авторизацияны қосу**, **Supabase қауіпсіздігін қатайту** және **деректі жоғалудан қорғау**. Орта мерзімде — тест жабыны мен файлды модульдеу.
